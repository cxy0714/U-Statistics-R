# Internal package environment

A dedicated environment for storing ustats package state, such as Python
availability checks. Parent is emptyenv() for isolation.

## Usage

``` r
ustats_env
```

## Format

An object of class `environment` of length 0.
